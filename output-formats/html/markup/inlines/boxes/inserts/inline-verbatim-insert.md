# Inline verbatim insert

**Note:** The following example assumes the content ``**content** of `someFile.um` that is *inserted*`` inside the file `someFile.um`.

```
[``Some file description](someFile.um)
```

renders to

```html
<pre>**content** of `someFile.um` that is *inserted*</pre>
```
