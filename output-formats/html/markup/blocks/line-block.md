# Line block

```
| Text where *spaces* are preserved as is.
|    All other **markup** however, is considered as **Unimarkup text**.
```

renders to

```html
<pre>Text where <em>spaces</em> are preserved as is.
   All other <strong>markup</strong> however, is considered as <strong>Unimarkup text</strong>.</pre>
```
